"""Basebreak provider adapters package.

Contains provider-specific adapters (e.g. Nebius Token Factory).
Provider-neutral domain, evidence, and security packages must never import from adapters.
"""
